CREATE TABLE "item" (
	`id`	INTEGER NOT NULL,
	`rarity`	INTEGER,
	`type`	INTEGER,
	`name`	INTEGER,
	`fire`	INTEGER,
	`torpedo`	INTEGER,
	`bomb`	INTEGER,
	`asw`	INTEGER,
	`aa`	INTEGER,
	`armor`	INTEGER,
	`evasion`	INTEGER,
	`hit`	INTEGER,
	`los`	INTEGER,
	`range`	INTEGER,
	`distance`	INTEGER,
	PRIMARY KEY(id)
)
